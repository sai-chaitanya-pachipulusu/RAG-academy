---
title: "QUCO-RAG Uncertainty"
description: "Implement uncertainty quantification for RAG responses"
---

# QUCO-RAG Uncertainty

Implement uncertainty quantification for RAG responses, allowing the system to express confidence levels and identify when it might be wrong.

## Requirements

1. Score retrieval confidence based on similarity distribution
2. Measure answer uncertainty using token-level probabilities
3. Combine into an overall confidence score
4. Flag low-confidence responses for human review

## Starter Code

```python
def quantify_uncertainty(query: str, retrieved_chunks: list[dict], answer: str) -> dict:
    """
    Quantify uncertainty in a RAG response.
    
    Returns:
        {
            "retrieval_confidence": float,  # 0-1
            "answer_confidence": float,     # 0-1
            "overall_confidence": float,    # 0-1
            "should_flag": bool
        }
    """
    # Your implementation here
    pass
```
